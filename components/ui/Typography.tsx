/**
 * Typography — Themed text component with variant presets (h1, h2, body1, caption, etc.)
 * Re-exports Text from common/ under the name the code generator expects.
 *
 * Usage:
 *   import { Typography } from '@/components/ui/Typography';
 *   <Typography variant="h1">Hello</Typography>
 *   <Typography variant="caption" color={colors.textSecondary}>Subtitle</Typography>
 */
export { Text as Typography } from '@/components/common/Text';
