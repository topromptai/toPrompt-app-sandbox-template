/**
 * Button — Re-export from common/ for @/components/ui/ import path compatibility.
 *
 * Usage:
 *   import { Button } from '@/components/ui/Button';
 */
export { Button } from '@/components/common/Button';
