/**
 * Screen — Full-screen wrapper with SafeAreaView, StatusBar, and optional scroll.
 * Re-exports SafeScreen from common/ under the name the code generator expects.
 *
 * Usage:
 *   import { Screen } from '@/components/ui/Screen';
 *   <Screen scroll>...content...</Screen>
 */
export { SafeScreen as Screen } from '@/components/common/SafeScreen';
