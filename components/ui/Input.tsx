/**
 * Input — Re-export from common/ for @/components/ui/ import path compatibility.
 *
 * Usage:
 *   import { Input } from '@/components/ui/Input';
 */
export { Input } from '@/components/common/Input';
