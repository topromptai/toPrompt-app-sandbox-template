/**
 * Modal — Re-export from common/ for @/components/ui/ import path compatibility.
 *
 * Usage:
 *   import { Modal } from '@/components/ui/Modal';
 */
export { Modal } from '@/components/common/Modal';
