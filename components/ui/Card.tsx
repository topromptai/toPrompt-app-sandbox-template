/**
 * Card — Re-export from common/ for @/components/ui/ import path compatibility.
 *
 * Usage:
 *   import { Card } from '@/components/ui/Card';
 */
export { Card } from '@/components/common/Card';
