/**
 * Barrel export for all design system constants.
 *
 * Usage:
 *   import { Colors, FontFamily, Spacing, Typography } from '@/constants';
 */

export { Colors, LightColors, DarkColors, type ColorScheme, type ThemeColors } from './colors';
export { FontFamily, FontWeight, fontAssets } from './fonts';
export { Spacing, SCREEN_PADDING, BorderRadius, type SpacingKey } from './spacing';
export { Typography, type TypographyVariant } from './typography';
